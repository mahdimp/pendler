name: Deploy to GitHub Pages

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - run: npm ci

      - run: npx ng build --base-href "/${{ github.event.repository.name }}/"

      # GitHub Pages has no server-side rewrites, so deep-linking into the SPA
      # (e.g. a bookmarked route) 404s unless we serve the app shell there too.
      - run: cp dist/pendler/browser/index.html dist/pendler/browser/404.html

      - uses: actions/upload-pages-artifact@v3
        with:
          path: dist/pendler/browser

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v4
